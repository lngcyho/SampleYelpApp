<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <LinearLayout
            android:layout_width= "match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/restaurant_details_image"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scaleType="centerCrop" />

            <include layout="@layout/content_restaurant_details_description"/>

            <include layout="@layout/content_restaurant_details_address_info"/>

            <!-- Clickable Widget List -->
            <include layout="@layout/content_restaurant_details_action_widgets" />

            <!-- Reviews Component-->
            <include layout="@layout/content_restaurant_details_reviews" />

            <!-- Other Buttons for Contact -->
            <include layout="@layout/content_restaurant_details_other_buttons" />
        </LinearLayout>
    </ScrollView>
</android.support.constraint.ConstraintLayout>